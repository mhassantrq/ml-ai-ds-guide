msg = """
        It is stated that many libraries used in the following preprocessing
        files, are separately explained in the libraries folder in root folder.
        It may help you to go through them beforehand.

        preprocessing step is a must before one can go on handling model training, or testing.
        even the choice of model depends on how the data may be preprocessed beforehand.

        preprocessing converts the unstructured or unusable data into useable data.
        data which can then be used by the algorithm.

        preprocessing may include many tasks such as remove stop words, stemming, lemmatization,
        tokenization, vectorization etc.
"""

print(msg)